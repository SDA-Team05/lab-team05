apiVersion: apps/v1
kind: Deployment
metadata:
  name: g05-lab04
  namespace: mzinga-lab4
  labels:
    app: g05-lab04
spec:
  # We want 3 replicas of our application to be running at all times
  replicas: 3
  selector:
    matchLabels:
      app: g05-lab04
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: g05-lab04
        version: v1
    spec:
      containers:
        - name: g05-lab04
          image: mzinga-webapp:1.0.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
          # Environment variables injected in the container
            - name: APP_VERSION
              value: "1.0.0"
            - name: APP_COLOR
              value: "blue"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"